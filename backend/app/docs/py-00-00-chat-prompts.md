I use your beeware-fastapi-mobile-enterprise as template and create myHomeTheater app by running 
python -m venv venv
source venv/bin/activate
pip install briefcase
briefcase new
then run briefcase dev and entering test as username and test as password
